$ const { GAM } = out.global;

$ const {
  name,
  size,
  aliases,
} = input;

<if(GAM)>
  <marko-web-gam-define-display-ad
    ...GAM.getAdUnit({ name, size, aliases })
    modifiers=input.modifiers
  />
</if>

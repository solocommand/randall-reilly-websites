import { getAsObject } from "@base-cms/object-path";

$ const content = getAsObject(input, "content");

<marko-web-load-more
  header=`More from ${content.name}`
  component-name="shared-content-card-deck-flow"
  component-input={ displayAds: false, nativeX: { enabled: false } }
  fragment-name="shared-content-list"
  query-name="all-author-content"
  query-params={ contactId: content.id, limit: 12 }
  page-input={ for: "content", id: content.id, type: content.type }
/>
